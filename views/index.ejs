<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <% messages.forEach(message => { %>
    <div class="message">
      <div class="message-text"><%= message.text %></div>
      <div class="message-user"><%= message.user %></div>
      <div class="message-added"><%= message.added %></div>
    </div>
    <% }) %>
  </body>
</html>
